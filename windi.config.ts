import { defineConfig } from 'windicss/helpers';

export default defineConfig({
  theme: {
    fontFamily: {
      'spacegrotesk': ['Space Grotesk'],
      'jost': ['Jost']
    },
    fontWeight: {
      'medium': 700,
      'bold': 900
    },
    colors: {
      primary: '#C48CF8',
      secondary: '#8CF893',
      tertiary: '#C5B8E6',
      text: '#FFFFFF',
      background: '#221F26',
    }
  }
})
